//! `historia restore <n> [path]` - restore the folder, or one file, to a past
//! snapshot (exact mirror of the tracked set, safety snapshot first). Implemented
//! in CP6.

pub fn run(_args: &[String]) -> Result<(), String> {
    Err("historia restore: not implemented yet (CP6)".to_string())
}
