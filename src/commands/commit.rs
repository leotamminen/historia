//! `historia commit -m "msg"` (alias: `snapshot`) - snapshot the whole folder.
//! Implemented in CP3.

pub fn run(_args: &[String]) -> Result<(), String> {
    Err("historia commit: not implemented yet (CP3)".to_string())
}
