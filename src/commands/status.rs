//! `historia status` - what changed since the last snapshot. Implemented in CP5.

pub fn run(_args: &[String]) -> Result<(), String> {
    Err("historia status: not implemented yet (CP5)".to_string())
}
